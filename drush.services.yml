services:
  helfi_gredi_image.metaupdater_commands:
    class: \Drupal\helfi_gredi_image\Commands\MetaUpdaterCommands
    arguments: [ '@helfi_gredi_image.asset_metadata.helper' ]
    tags:
      - { name: drush.command }
